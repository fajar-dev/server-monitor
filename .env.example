# Copy to .env and fill in. Used by docker-compose for the Loki Basic Auth proxy.
# Promtail clients on other servers must send these same credentials.
LOKI_USER=loki
LOKI_PASSWORD=change-me-to-a-strong-password
